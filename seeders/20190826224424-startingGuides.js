'use strict';

module.exports = {
  up: (queryInterface, Sequelize) => {
    return queryInterface.bulkInsert('Guides', [
      {

      }
    ])
  },

  down: (queryInterface, Sequelize) => {
   
  }
};
