let models;_d9f‍.x([["default",()=>GuideController]]);_d9f‍.w("../models",[["default",["models"],function(v){models=v}]]);

               class GuideController {
  
  static async getGuides(req,res){

    models.Guide.findAll({where: {id: 1}}).then(data => res.json(data)).catch(err => res.status(404).send(err));
  }

  static async postGuide(req, res){

    const { firstName, lastName, email, phone_number } = req.body;

    models.Guide.create({ 
        first_name: firstName, 
        last_name: lastName, 
        email: email, 
        phone_number: phone_number, 
        rating: 0,
        joined: new Date()}
    ).then(data => res.send(data)).catch(err => res.send(err));
  }

  static async getGuideById(req, res){
    res.send(`User Nr. ${req.params.id}.`);
  }

  static async getAllJobsByGuide(req, res){
    res.send("Here are some jobs");
  }
  
}